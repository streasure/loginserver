log:
  level: info
  format: json
  console:
    enabled: false
  file:
    enabled: true
    path: ./logs/loginserver.log
    rotate:
      max_size: 500
      max_backups: 10
      max_age: 30
  async:
    enabled: true
    buffer_size: 100000
    batch_size: 2000
    flush_interval: 10
